---
interface Props { items: { q: string; a: string }[] }
const { items } = Astro.props;
---
<div class="faq">
  {items.map((f) => (
    <details>
      <summary>{f.q}</summary>
      <div class="faq__a"><p>{f.a}</p></div>
    </details>
  ))}
</div>
